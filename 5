
#include <stdio.h>
#include <math.h>

void main()
{
    float x = 3.241, a, y, step = (6.3 - 1.15)/9;
    
    for(a = 1.15; a<=6.3; a = a+step){
    
    y = atan(a*a + x*x)/a*exp(x/2);
    
        if(y>1.5 && y<2.5){
            printf("\ny  = %f", y);
        }
    }
  
}
